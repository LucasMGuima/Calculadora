#ifndef _OPERATIONS_H_
#define _OPERATIONS_H_

#define TYPE float

/* a + b */
TYPE sum(TYPE a, TYPE b);
/* a - b */
TYPE sub(TYPE a, TYPE b);
/* a * b */
TYPE mult(TYPE a, TYPE b);
/* a / b */
TYPE divi(TYPE a, TYPE b);
/* raiz b de a*/
TYPE root(TYPE a, TYPE b);
/* a elevado b */
TYPE power(TYPE a, TYPE b);

#endif /* _OPERATION_H_ */